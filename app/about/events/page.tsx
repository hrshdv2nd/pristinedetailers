import { AboutEvents } from '@/components/pages/about-events';

export const metadata = {
  title: 'Events — Pristine Detailers',
  description: 'Member collection days, VIP reveals, and seasonal car showcases across Melbourne.',
};

export default function Page() {
  return <AboutEvents />;
}
