import { AboutResults } from '@/components/pages/about-results';

export const metadata = {
  title: 'Results — Pristine Detailers',
  description: 'Ceramic coatings, PPF wraps, and full-detail transformations — the proof is in the finish.',
};

export default function Page() {
  return <AboutResults />;
}
